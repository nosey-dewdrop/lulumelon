"""lulumelon: how visible a brand is inside an answer engine, with the uncertainty attached.

Two halves and a wall between them.

`mirror` is arithmetic over runs that already exist. It never touches the
network and is reproducible from a seed, which is what makes it an instrument
rather than a dashboard.

`collect` is the only part that talks to anything we do not control, and it
computes nothing. A number that came out of this library can always be traced
back to lines in a ledger that were written before any arithmetic happened.

`keys`, `prices` and `cli` are the path a person walks before either half runs:
find a key, know what a call costs, and be told which of those is missing.
"""

__all__ = ["cli", "collect", "keys", "mirror", "panel", "prices"]
